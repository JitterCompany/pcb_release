# Interactive PCB release script

Run `release_pcb.py` in the same directory as your KiCad project. It will guide you step-by-step to export the right files and creates a ZIP file with all gerber/BOM/assembly data to get a board assembled.
